<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kbo.domain.ranking.repository.RankingMapper">
    <select id="selectRankList" resultType="com.kbo.domain.ranking.dto.RankingDTO">
        SELECT RANK_SEQ AS RANK_SEQ,
               CLUB_CODE AS CLUB_CODE,
               GAMES AS GAMES,
               WINS AS WINS,
               LOSES AS LOSES,
               DRAWS AS DRAWS,
               PERCENTAGE AS PERCENTAGE,
               GAME_BEHIND AS GAME_BEHIND
        FROM  RANK
    </select>
</mapper>